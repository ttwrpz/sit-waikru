interface FormData {
    name: string;
    major: string;
    message: string;
}

interface CanvasDimensions {
    width: number;
    height: number;
}

export type {
    FormData,
    CanvasDimensions
}